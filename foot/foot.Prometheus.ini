# Prometheus (Yoga): 3200x2000 internal panel driven at scale 1, so font
# sizes are in physical pixels and have to be much larger than elsewhere.
# Linked over foot.ini by home/Prometheus.nix in the nixos repository.
font=monospace:size=26

[cursor]
blink=no
