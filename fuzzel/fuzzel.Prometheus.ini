# Prometheus (Yoga). Its 3200x2000 panel runs at scale 1, so tune this
# independently from the shared default. Fuzzel sizes its window from the
# font, so this controls the launcher's width as well as its text.
# Selected as fuzzel.ini by home/Prometheus.nix.
font=monospace:size=10
