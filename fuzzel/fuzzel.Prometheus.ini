# Prometheus (Yoga). Deliberately small: fuzzel sizes its window from the
# font, so a large size here produces an unusably wide launcher on the
# 3200x2000 panel. Linked over fuzzel.ini by home/Prometheus.nix.
font=monospace:size=26
