# Prometheus (Yoga). The 3200x2000 panel runs at scale 1, so the font must be
# larger than on the other machines. Fuzzel sizes its window from the font,
# meaning this value widens the launcher as well as enlarging its text.
# Linked over fuzzel.ini by home/Prometheus.nix.
font=monospace:size=26
